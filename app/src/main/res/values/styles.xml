<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:buttonStyle">@style/RobotoButtonStyle</item>
        <item name="android:editTextStyle">@style/RobotoEditStyle</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.NoActionBar.Green">
        <item name="android:colorPrimaryDark">@color/colorPrimaryGreen</item>
    </style>

    <style name="AppTheme.NoActionBar.HongBao">
        <item name="android:colorPrimary">@color/colorHongBaoRed</item>
        <item name="android:colorPrimaryDark">@color/colorHongBaoRed</item>
    </style>

    <style name="AppTheme.FullScreen">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="RobotoButtonStyle" parent="android:Widget.Holo.Button">
    </style>

    <style name="RobotoEditStyle" parent="android:Widget.Holo.EditText">
        <item name="android:background">@drawable/selector_edit_gray</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textColorHint">@color/colorAccent</item>
        <item name="android:padding">12dp</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="MTCSeekBarStyle" parent="android:Widget.SeekBar">
        <item name="android:progressDrawable">@drawable/mtc_seekbar</item>
        <item name="android:thumb">@drawable/shape_seekbar_point</item>
        <item name="android:minHeight">6dp</item>
        <item name="android:maxHeight">6dp</item>
    </style>

</resources>
